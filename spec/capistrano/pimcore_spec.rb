require 'spec_helper'

describe Capistrano::Pimcore5 do
  it 'has a version number' do
    expect(Capistrano::Pimcore5::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
